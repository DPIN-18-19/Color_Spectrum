using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class CreateScoreList
{
    /*[MenuItem("Assets/Create/ScoreList")]
    public static ScoreList Create()
    {
        ScoreList asset = ScriptableObject.CreateInstance<ScoreList>();
        AssetDatabase.CreateAsset(asset, "Assets/ScoreList.asset");
        AssetDatabase.SaveAssets();
        return asset;
    }
    */
}
