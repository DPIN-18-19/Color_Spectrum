using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NoCambiarColor : MonoBehaviour {
   // public ColorChangingController cambioColor;
   // public SameColorPink ParedRosa;
   // public float DuracionMismoColor;
   // private float MaxDuracion;
    // Use this for initialization
    void Start () {
     //   MaxDuracion = DuracionMismoColor;
    }
	
	// Update is called once per frame
	void Update () {
	//	if(ParedRosa.ParedCambioNo == true)
       // {
       //     DuracionMismoColor -= Time.deltaTime;
       //     if (DuracionMismoColor < MaxDuracion)
       //     {
         //       cambioColor.SetColor(2);
         //       Debug.Log("CambiaUnRATO");
         //   }

            //else
              // DuracionMismoColor = 0;


        }
	}

